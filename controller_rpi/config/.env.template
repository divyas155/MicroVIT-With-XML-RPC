# Controller AI Service Configuration
# Copy to .env and set values. MQTT broker must be running (e.g. Mosquitto on Controller).

# Controller Configuration
CONTROLLER_ID=central_controller

# Ollama AI Configuration - use Orin's Ollama (recommended; Orin already runs it for Robot1)
# Set to Orin IP so Controller doesn't need to run Ollama on Pi (Pi is too slow)
OLLAMA_HOST=http://10.13.68.159:11434
OLLAMA_MODEL=qwen2.5:0.5b

# MQTT Configuration (broker: typically this machine for Controller)
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
MQTT_BROKER_USERNAME=
MQTT_BROKER_PASSWORD=

# Logging Configuration
LOG_LEVEL=INFO
LOG_DIR=./logs

# AI Decision Configuration
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=200
AI_TIMEOUT=30

